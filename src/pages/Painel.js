import React from "react";
import { Button } from '@material-ui/core';

export const Painel = () => {
    return (
        <div>
            <Button color="primary">Hello World</Button>
        </div>
    );
};
