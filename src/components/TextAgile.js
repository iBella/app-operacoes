import React from "react";

export const TextAgile = (props) => {

    return (
        <div>
            <h3 className="text-agile">{props.phrase}</h3>
            <hr></hr>
        </div>
    );
};
