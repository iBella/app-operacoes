export enum TipoAcao {
    infra = 0,
    po = 1,
    sm = 2,
    rito = 3,
    squad = 4,
    cliente = 5
}

export enum ResponsavelAcao {
    dti = 0,
    cliente = 1
}

export enum PrioridadeAcao {
    baixa = 0,
    media = 1,
    alta = 2,
    muitoAlta = 3
}

export enum FuncaoMembro {
    desenvolvedor = 0,
    dl = 1,
    arquiteto = 2,
    po = 3,
    sm = 4
}